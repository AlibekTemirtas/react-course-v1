import {Layout} from "../Layout/Layout.jsx";
import {RestaurantPage} from "../Restaurant/restaurant-page.jsx";

export const App = () => {
    return (
        <Layout>
            <RestaurantPage />
        </Layout>
    )
}
